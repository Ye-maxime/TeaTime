import React from 'react';
import MenuList from '../components/MenuList';

const Menu = props => {
    return (
        <div className="custom-content">
            <MenuList />
        </div>
    );
}

export default Menu;
